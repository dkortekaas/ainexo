// Application configuration
const config = {
  appTitle: "Ainexo",
  appLogo: "/ainexo-logo.png", // Optimized from 74KB to 3.9KB
  appIcon: "/favicon.png", // Optimized from 500KB to 1.2KB
  email: "hello@ainexo.app",
  itemsPerPage: 5,
  appUrl: process.env.NEXT_PUBLIC_APP_URL || "https://ainexo.app",
};

export default config;
